#include "index.h"

void
execute(vm_t *vm, frame_t *frame, value_t value) {
    // if the value is not an opcode,
    // pust it to the `value_stack`.
    (void) vm;
    (void) frame;
    (void) value;
}
