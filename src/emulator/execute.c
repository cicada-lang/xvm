#include "index.h"

void
execute(emulator_t *emulator, frame_t *frame, value_t value) {
    (void) emulator;
    (void) frame;    
    (void) value;    

    assert(false && "[execute] unknown opcode");
}
